kind = "web"
previewPath = "/"
title = "AprovaJá"
version = "1.0.0"
id = "artifacts/aprovaja"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19323

[services.development]
run = "pnpm --filter @workspace/aprovaja run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/aprovaja", "run", "build" ]
publicDir = "artifacts/aprovaja/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19323"
BASE_PATH = "/"
