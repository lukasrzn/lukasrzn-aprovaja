You are a senior full-stack SaaS engineer and authentication flow specialist responsible for fixing a critical user routing issue in the “AprovaJá” platform.====================================================CRITICAL BUG (AUTH FLOW REDIRECT ISSUE)====================================================There is a major problem in the login/authentication flow:- Users who ALREADY have an account are being incorrectly redirected to the “Plans / Subscription” page.- This is happening even after they successfully enter correct login credentials.- This breaks the expected authentication flow and creates a poor user experience.This is a HIGH PRIORITY BUG and must be fixed immediately.====================================================YOUR MISSION====================================================You must:1. Fix the login authentication redirect logic2. Ensure users with valid accounts go directly to the dashboard3. Ensure ONLY non-subscribed or unauthenticated users go to plans page4. Separate authentication flow from subscription flow correctly====================================================EXPECTED CORRECT FLOW====================================================A) USER WITH EXISTING ACCOUNT + ACTIVE SESSION:- Enters email and password- Authenticated successfully- Redirect immediately to:  → /dashboardB) USER WITH ACCOUNT BUT NO ACTIVE PLAN:- After login:  → redirect to /planosC) NEW USER (NO ACCOUNT):- Redirect to signup flow====================================================CURRENT BROKEN BEHAVIOR====================================================- All users (even logged-in users) are being sent to:  → /planos (INCORRECT)This must be fixed.====================================================ROOT CAUSE AREAS TO CHECK====================================================Investigate and fix:- Authentication middleware logic- Session validation handling- JWT/token decoding issues- Frontend redirect conditions- Subscription check running BEFORE auth check- Incorrect routing guards- Global redirect interceptor overriding login flow====================================================FRONTEND REQUIREMENTS====================================================- Ensure login success triggers correct routing:  if (userAuthenticated && subscriptionActive) → /dashboard  if (userAuthenticated && !subscriptionActive) → /planos- Remove any forced redirect to /planos on login success- Ensure login state is properly stored before routing====================================================BACKEND REQUIREMENTS====================================================- Return correct user auth status:  - authenticated: true/false  - subscriptionStatus: active/inactive  - role: user/admin- Ensure API does NOT force subscription redirect====================================================SECURITY REQUIREMENTS====================================================- Do not allow unauthorized access to /dashboard- Do not block authenticated users incorrectly- Ensure role + subscription checks are separate layers====================================================USER EXPERIENCE====================================================Fix flow so that:- Login feels instant and smooth- No unexpected redirects- Clear separation between login and subscription prompts- No confusion for returning users====================================================FINAL GOAL====================================================After fix:- Users with valid accounts ALWAYS enter the dashboard correctly- Only non-subscribed users are redirected to plans- Login system is stable, predictable, and production-ready- Authentication flow is clean and fully separated from billing flowGenerate production-ready fixes for frontend routing, backend auth logic, and middleware validation.